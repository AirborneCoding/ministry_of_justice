import GlobalIndicators from "@/components/satistics/GlobalIndicators";
import React from "react";

const page = () => {
    return (
        <main className="min-h-screen body-container">
            <GlobalIndicators />
        </main>
    )
};

export default page;
